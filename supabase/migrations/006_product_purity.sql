-- ============================================================================
-- 006. 품목 순도(purity) 컬럼 추가
-- 적용: Supabase Dashboard → SQL Editor 에 붙여넣고 RUN
-- ----------------------------------------------------------------------------
-- 품목 = 종류(raw/oil/water) × 사이즈(thickness) × 순도(purity) 조합.
-- 예: '99.3% 합금', '99.99% 순알'
-- ============================================================================

ALTER TABLE products ADD COLUMN IF NOT EXISTS purity VARCHAR(30);
